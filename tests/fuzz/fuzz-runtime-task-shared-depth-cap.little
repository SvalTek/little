var root = {}
var cursor = root
var i = 0

while i < 260 {
    var next = {}
    cursor.next = next
    cursor = next
    i = i + 1
}

task.run(fn(state) {
    return state
}, root).catch(fn(reason) {
    io.print(string.contains(reason, "maximum nesting depth"))
})
