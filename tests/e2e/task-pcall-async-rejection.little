class Token {}

var result = pcall(fn() {
    return task.run(fn(state) { return state.item }, { item: Token() })
})

io.print(result.ok)
io.print(type result.value)
result.value.catch(fn(reason) {
    io.print(string.contains(reason, "Unsupported"))
})
